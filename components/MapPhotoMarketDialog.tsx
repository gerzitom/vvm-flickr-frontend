import React, {FC} from 'react'
import {PhotoSortWrapper} from "../types";

type Props = {
    wrapper: PhotoSortWrapper
}
export const MapPhotoMarketDialog: FC<Props> = ({wrapper}) => {
  return (
    <>
    </>
  )
}